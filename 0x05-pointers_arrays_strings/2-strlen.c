#include <stdio.h>
/**
 * _strlen - Returns the lenght of string
 * @str: the string
 * Return: The lenght of @str
*/

size_t _strlen(const char *str)
{
size_t lenght = 0;
while (*str++)
lenght++;
return (length);
}
